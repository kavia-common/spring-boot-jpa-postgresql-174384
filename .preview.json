{
  "primary": {
    "command": "./mvnw spring-boot:run -Dspring-boot.run.arguments=--server.port=3001 --server.address=0.0.0.0",
    "port": 3001,
    "env": {
      "JAVA_TOOL_OPTIONS": "-XX:+UseContainerSupport"
    },
    "notes": "Preview must use Maven Wrapper only. No fallback to system 'mvn'. Ensure './mvnw' is executable (chmod +x ./mvnw)."
  }
}
