{
  "name": "spring-boot-jpa-postgresql",
  "command": "bash ./run.sh",
  "port": 3001,
  "env": {
    "JAVA_TOOL_OPTIONS": "-XX:+UseContainerSupport"
  },
  "notes": "Preview must execute bash ./run.sh which enforces using ./mvnw and never plain 'mvn'."
}
