{
  "name": "spring-boot-jpa-postgresql",
  "command": "./mvnw spring-boot:run -Dspring-boot.run.arguments=--server.port=3001 --server.address=0.0.0.0",
  "port": 3001,
  "env": {
    "JAVA_TOOL_OPTIONS": "-XX:+UseContainerSupport"
  },
  "notes": "Primary start uses Maven Wrapper directly. You can also 'bash ./run.sh' for profile-based runs; both use ./mvnw. No system 'mvn' is used."
}
